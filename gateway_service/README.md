# Gateway Service

Nginx-based API Gateway.

## Routes

- `/register`, `/login`, `/verify` -> Auth Service
- `/orders` -> Orders Service
- `/items` -> Warehouse Service
- `/tracking` -> Tracking Service
